# Правила проекта

## Стиль комментариев в коде
- Обычные комментарии в коде (строчные `//` или блочные `/* ... */`, за исключением комментариев JSDoc `/** ... */`) должны быть написаны со строчной (маленькой) буквы и без точки на конце.
